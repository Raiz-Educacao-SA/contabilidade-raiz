"use client";
import {useMemo,useState} from "react";import{Calculator,FileCheck2,RefreshCw,TriangleAlert}from"lucide-react";
type F={id:string;ra:string;name:string;status:string;originalValue:number;discount:number};type C={id:string;ra:string;name:string;value:number;kind:"revenue"|"discount";isReversal:boolean};const brl=new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}),tol=.01;
export default function RevenueReconciliation({companyCode,companyName,competence,accessToken}:{companyCode:string;companyName:string;competence:string;accessToken:string}){const[f,setF]=useState<F[]>([]),[c,setC]=useState<C[]>([]),[fr,setFr]=useState(false),[cr,setCr]=useState(false),[loading,setLoading]=useState<"fiscal"|"accounting"|null>(null),[error,setError]=useState("");async function update(source:"fiscal"|"accounting"){setLoading(source);setError("");try{const r=await fetch(`/api/totvs/revenue-reconciliation?company=${companyCode}&competence=${competence}&source=${source}`,{headers:{authorization:`Bearer ${accessToken}`},cache:"no-store"}),p=await r.json();if(!r.ok)throw new Error(p.error||"Falha ao atualizar a base.");if(source==="fiscal"){setF(p.rows||[]);setFr(true)}else{setC(p.rows||[]);setCr(true)}}catch(e){setError((e as Error).message)}finally{setLoading(null)}}
const rows=useMemo(()=>{const fm=new Map<string,{name:string;status:string;rev:number;disc:number}>();f.forEach(x=>{const a=fm.get(x.ra)||{name:x.name,status:x.status,rev:0,disc:0};a.rev+=x.originalValue;a.disc+=x.discount;fm.set(x.ra,a)});const cm=new Map<string,{name:string;rev:number;disc:number}>();c.forEach(x=>{const a=cm.get(x.ra)||{name:x.name,rev:0,disc:0};if(x.kind==="revenue")a.rev+=x.value;else a.disc+=x.value;cm.set(x.ra,a)});return[...new Set([...fm.keys(),...cm.keys()])].map(ra=>{const a=fm.get(ra),b=cm.get(ra),dr=(b?.rev||0)-(a?.rev||0),dd=(b?.disc||0)-(a?.disc||0),status=!a?"Só no Contábil":!b?"Só no Fiscal":Math.abs(dr)<=tol&&Math.abs(dd)<=tol?"Conciliado":"Divergente";return{ra,name:a?.name||b?.name||"",fiscalStatus:a?.status||"",fiscalRevenue:a?.rev||0,accountingRevenue:b?.rev||0,revenueDifference:dr,fiscalDiscount:a?.disc||0,accountingDiscount:b?.disc||0,discountDifference:dd,impact:dr-dd,status,comment:status!=="Divergente"?status:Math.abs(dr)>tol&&Math.abs(dd)>tol?"Verificar receita e desconto":Math.abs(dr)>tol?"Verificar diferença de receita":"Verificar diferença de desconto"}})},[f,c]);const pending=rows.filter(x=>x.status!=="Conciliado"),reconciled=rows.length-pending.length,fRev=f.reduce((s,x)=>s+x.originalValue,0),cRev=c.filter(x=>x.kind==="revenue").reduce((s,x)=>s+x.value,0),fDisc=f.reduce((s,x)=>s+x.discount,0),cDisc=c.filter(x=>x.kind==="discount").reduce((s,x)=>s+x.value,0);
return <section className="panel revenue-panel"><div className="revenue-head"><div><span className="eyebrow">CONCILIAÇÃO DE RECEITA</span><h2>{companyName}</h2><p>RA + competência {competence.split("-").reverse().join("/")} · tolerância R$ 0,01</p></div><div className="revenue-actions"><button className="fiscal-button" disabled={loading!==null} onClick={()=>void update("fiscal")}><FileCheck2/>{loading==="fiscal"?"Atualizando...":"Atualizar base fiscal"}</button><button className="accounting-button" disabled={loading!==null} onClick={()=>void update("accounting")}><Calculator/>{loading==="accounting"?"Atualizando...":"Atualizar base contábil"}</button></div></div>{error&&<div className="notice error">{error}</div>}<div className="revenue-status"><article><span>Receita fiscal</span><b>{fr?brl.format(fRev):"Aguardando"}</b><small>Planilha Net 53</small></article><article><span>Receita contábil</span><b>{cr?brl.format(cRev):"Aguardando"}</b><small>Grupo 3.1.1.01.01</small></article><article><span>Desconto fiscal × contábil</span><b>{fr&&cr?`${brl.format(fDisc)} × ${brl.format(cDisc)}`:"Aguardando"}</b><small>Bolsas e descontos</small></article><article><span>Conciliados</span><b>{fr&&cr?reconciled:"—"}</b><small>RA sem diferença</small></article><article className={pending.length?"has-warning":""}><span>Inconsistências</span><b>{fr&&cr?pending.length:"—"}</b><small>Para tratamento</small></article></div>{!fr||!cr?<div className="revenue-empty"><RefreshCw/><b>Atualize as duas bases</b><span>O cruzamento mensal será executado por RA + competência.</span></div>:pending.length?<><div className="revenue-warning"><TriangleAlert/><div><b>{pending.length} inconsistência(s) para tratamento</b><span>Registros conciliados não aparecem na lista.</span></div></div><div className="table-wrap revenue-table"><table><thead><tr><th>Status</th><th>RA</th><th>Aluno</th><th>Status fiscal</th><th>Receita fiscal</th><th>Receita contábil</th><th>Δ Receita</th><th>Desconto fiscal</th><th>Desconto contábil</th><th>Δ Desconto</th><th>Impacto</th><th>Orientação</th></tr></thead><tbody>{pending.map(x=><tr key={x.ra}><td><span className="revenue-badge">{x.status}</span></td><td><b>{x.ra}</b></td><td>{x.name||"—"}</td><td>{x.fiscalStatus||"—"}</td><td>{brl.format(x.fiscalRevenue)}</td><td>{brl.format(x.accountingRevenue)}</td><td className={Math.abs(x.revenueDifference)>tol?"negative":""}>{brl.format(x.revenueDifference)}</td><td>{brl.format(x.fiscalDiscount)}</td><td>{brl.format(x.accountingDiscount)}</td><td className={Math.abs(x.discountDifference)>tol?"negative":""}>{brl.format(x.discountDifference)}</td><td className={Math.abs(x.impact)>tol?"negative":""}><b>{brl.format(x.impact)}</b></td><td>{x.comment}</td></tr>)}</tbody></table></div></>:<div className="revenue-empty success"><FileCheck2/><b>Bases conciliadas</b><span>Nenhuma inconsistência encontrada.</span></div>}</section>}
